#include <iostream>
#include "heap.h"


int main() {
  // create custom heap
  Heap<int, std::less<int>> h1(2, std::less<int>());

  // push some random items

  // 1. make sure they are popped in order

  return 0;
}